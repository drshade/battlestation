[package]
name = "bsctl"
version = "0.1.0"
edition = "2024"
description = "battlestation repo control tool — claude-ws status protocol (hook writer + poller)"
publish = false

[dependencies]
serde_json = "1"

[profile.release]
strip = true
